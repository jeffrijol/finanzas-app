Plan de migración:

    Mantener la configuración actual de Tailwind CSS y ShadCN

    Migrar de .astro a .tsx/.ts

    Configurar Vite con TypeScript

    Conservar la misma estructura de componentes

2. Librerías de Estado y Datos - CONFIRMADAS

✅ SÍ a @tanstack/react-query:

    Para cache, sincronización y estados de carga/error

    Implementar QueryClientProvider en el nivel raíz

    Usar useQuery para GET y useMutation para POST/PUT/DELETE

    Invalidación automática después de mutaciones

✅ SÍ a Zustand (ligero):

    Solo para estado global mínimo: items y configuración de usuario

    No para estado local de componentes

    Implementar store separado para useItemsStore y useUIStore

3. Tipos TypeScript - ALINEADOS CON BACKEND

Confirmo los tipos propuestos. Actualización importante:
typescript

// Incluir estos campos que ya existen en el backend:
interface Transaction {
  id: string;
  fechaValor: string;  // ISO string desde backend
  categoria: string;
  descripcion: string;
  importe: number;
  saldo: number;
  itemAsignadoId: string | null;  // ✅ nullable
  metadata: string | null;         // ✅ nullable
  excelUploadId: string | null;    // ✅ nullable
  createdAt: string;
  updatedAt: string;
  // Relaciones que el backend puede incluir
  itemAsignado?: Item | null;
  excelUpload?: ExcelUpload | null;
}

interface Item {
  id: string;
  nombre: string;
  descripcion: string | null;
  color: string | null;
  icono: string | null;
  activo: boolean;
  createdAt: string;
  updatedAt: string;
}

4. API Client Layer - IMPLEMENTAR COMO CLASE

✅ Aprobada la clase ApiClient con estos métodos específicos:
typescript

class ApiClient {
  // Endpoints específicos para nuestra app
  async uploadExcel(file: File): Promise<UploadResponse> { ... }
  async getTransactions(params: GetTransactionsParams): Promise<PaginatedResponse<Transaction>> { ... }
  async assignItem(transactionId: string, itemId: string | null): Promise<Transaction> { ... }
  async getItems(): Promise<Item[]> { ... }
  
  // Métodos genéricos
  private async request<T>(endpoint: string, options: RequestInit): Promise<T> { ... }
}

5. Plan de Implementación - CONFIRMADO CON AJUSTES

Fase 1 - Setup Base (AÑADIR):
bash

# Instalar dependencias completas
pnpm add @tanstack/react-query @tanstack/react-table zustand axios date-fns lucide-react

Fase 2 - Componentes (PRIORIDAD):

    ApiClient y configuración de React Query

    DashboardLayout con sidebar responsive

    FileUploadCard con drag & drop funcional

    ItemsPanel que se actualice en tiempo real

Fase 3 - Tabla (CRÍTICO):

    Usar @tanstack/react-table para funcionalidad avanzada

    Implementar useTransactionsTable hook personalizado

    Actualización optimista con rollback automático

6. CORS y Variables de Entorno - CONFIGURAR

Confirmo que el backend ya tiene CORS configurado. En el frontend:
env

# .env.local
VITE_API_URL=http://localhost:3001/api
VITE_APP_TITLE="Transaction Dashboard"

7. Diseño UI - MANTENER PROPUESTA ORIGINAL

✅ Aprobado todo el diseño propuesto:

    Paleta de colores oscura (slate-950)

    Tabla estilo fintech con hover states

    Color coding por items

    Badges y estados visuales

    Responsive design con breakpoints específicos

8. Requisitos Específicos Adicionales

Añadir estos componentes extra:

    TransactionFilters.tsx - Barra de filtros avanzada

    BulkActions.tsx - Para asignación masiva de items (ctrl+click)

    TransactionDetails.tsx - Modal con metadata completa

    ImportHistory.tsx - Lista de archivos subidos anteriormente

Validaciones importantes:

    ✅ Límite de tamaño de archivo (10MB)

    ✅ Formato de archivo (.xlsx, .csv)

    ✅ Toast notifications para todas las acciones

    ✅ Loading skeletons para tabla y cards

    ✅ Error boundaries para componentes críticos

9. Próximos Pasos Inmediatos

Para que empieces a generar código, necesito:

    Estructura inicial de proyecto Vite + React:

        vite.config.ts con alias y plugins

        tsconfig.json con paths configurados

        Configuración de Tailwind y ShadCN

    Archivos base:

        src/main.tsx con providers (QueryClientProvider, etc.)

        src/App.tsx con router básico

        src/lib/api-client.ts con la clase ApiClient

        src/types/index.ts con todos los tipos

    Primer componente:

        DashboardLayout.tsx con sidebar y header





