⚠️ Vulnerabilidad de Fuerza Bruta - PARCIALMENTE RESUELTO

Backend: ✅ Rate limiting en authLimiter (5 intentos/15min)
Frontend: ❌ Falta rate limiting en UI - Pueden enviarse requests manualmente
typescript

// Recomendación adicional para frontend:
const useAuthWithRateLimit = () => {
  const [attempts, setAttempts] = useState(0);
  const [lockedUntil, setLockedUntil] = useState<number | null>(null);
  
  // Bloquear UI después de 3 intentos fallidos
  if (lockedUntil && Date.now() < lockedUntil) {
    throw new Error('Demasiados intentos. Espera 15 minutos.');
  }
};

